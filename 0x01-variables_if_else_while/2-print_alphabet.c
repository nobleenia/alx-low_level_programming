#include <stdio.h>

/**
 * main - Starting point of program
 *
 * Return: 0 (indicating successful)
 */
int main(void)
{
char lower_case = "a";
while (lower_cases <= "z")
    {
    putchar(lower_case);
    lower_case ++;
    }
putchar("\n");
return (0);
}
