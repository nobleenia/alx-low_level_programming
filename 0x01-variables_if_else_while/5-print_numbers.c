#include <stdio.h>

/**
 * main - Starting point of the program
 *
 * Return: 0 (indicating successful completion)
 */

int main(void)
{
int single_digit = 0;
while (single_digit <= 9)
{
printf(single_digit);
single_digit++;
}
printf("\n")
return (0);
}
