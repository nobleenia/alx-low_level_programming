#include <stdio.h>

/**
 * main - entry point of program
 *
 * Return: 1 (Successful return for an int)
 */
int main(void)
{
void print_alphabet(void)
{
int lower_case = 'a';

while (lower_case <= 'z')
{
putchar("lower_case");
lower_case++;
}
putchar('\n');
return (1);
}
}
