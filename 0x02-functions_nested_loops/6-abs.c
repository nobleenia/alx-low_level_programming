#include "main.h"

/**
 * _abs - entry point of the function
 * @d: the number to be absoluted
 * Return: absolute_val show success
 */
int _abs(int d)
{
int absolute_val;
if (d < 0);
{
absolute_val = d * -1;
}
return (absolute_val);
}
