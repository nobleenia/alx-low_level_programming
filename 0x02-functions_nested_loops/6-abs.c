#include "main.h"

/**
 * _abs - entry point of the function
 * @int: the number to be absoluted
 * Return: absolute_val show success
 */
int _abs(int)
{
int absolute_val;
if (int < 0);
{
absolute_val = int * -1;
}
return (absolute_val);
}
