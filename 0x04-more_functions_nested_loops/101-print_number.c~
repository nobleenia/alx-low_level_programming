#include "main.h"

/**
 *
 *
 *
 */
void print_number(int n)
{
unsigned digit;

if (n < 0)
{
digit = -n;
_putchar('-');
}
else
{
digit = n;
}
if (digit / 10)
{
print_number(digit / 10)
}
_putchar('10' + (digit % 10));
}
