#include "main.h"

/**
 * mul - the starting point of the function
 * @a: the first integer to be multiplied
 * @b: the second digit to be multiplied
 * Return: the product value
 */
int mul(int a, int b)
{
int product = a * b;
return (product);
}
