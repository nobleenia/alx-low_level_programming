#include <stdio.h>
#include <stdlib.h>

/**
 *
 *
 *
 *
 *
 */
int main(int argc, char **argv)
{
int n = 0;
int t;
if (argc < 2)
{
printf("0\n");
}
for (t = 1; t < argc; t++)
{
if (argv[t] == '-')
{
printf("Error");
return (1);
}
else
{
n += atoi(argv[t]);
}
printf("%d\n", n);
}
return (0);
}
